<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.votinginfoproject.VotingInformationProject"
          xmlns:android="http://schemas.android.com/apk/res/android"
          android:installLocation="auto">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:name=".application.VIPApp"
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.Vip_tab_bar"
        android:versionCode="7"
        android:versionName="3.4.0">

        <activity
            android:name=".activities.HomeActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.VIPTabBarActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/activity.vip.tab"
            android:parentActivityName=".activities.HomeActivity"
            android:screenOrientation="portrait">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.HomeActivity"/>
        </activity>

        <activity
            android:name=".activities.AboutActivity"
            android:label="@string/activity.title.about"
            android:parentActivityName=".activities.VIPTabBarActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.votinginfoproject.VotingInformationProject.activities.VIPTabBarActivity"/>
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <!-- Google Maps API Key required. Please reference the "Adding API keys for the app" section of the Readme for more details. -->

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/google_api_android_key"/>
    </application>
</manifest>
